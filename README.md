# tobil.dev

This site is made using the [hhp](https://github.com/RealyUniqueName/HHP) template tool.

```sh
haxelib install generate.hxml
haxe generate.hxml
```

Can be deployed locally using `nekotools` or any http server.

```sh
nekotools server -d www
```
